package com.chan.pf4j.utils;

public class PluginUtil {

  private PluginUtil() {
    String exceptionMessage = "Don't Create Utility class";
    throw new IllegalArgumentException(exceptionMessage);
  }

}
